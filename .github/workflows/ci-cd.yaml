name: CI/CD

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    environment: development
    name: "Build"
    steps:
      - name: Say hi
        run: echo hello

  sonarqube:
    name: SonarQube
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/sonarqube.yml
    secrets:
      sonar_token: ${{ secrets.SONAR_TOKEN }}
    with:
      sonar_host_url: ${{ vars.SONAR_HOST_URL }}
      python_version: ${{ vars.PYTHON_VERSION }}
      srcs_paths: "src/"            # Optional
      tests_path: "tests/"          # Optional
      coverage_path: "coverage.xml" # Optional
    needs: build
